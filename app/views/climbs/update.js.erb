console.log('goodbye from ajax');

// Replace the text.
newDesc = "<%= @climb.description %>";
document.getElementById('climb-desc').innerHTML = newDesc;

// Hide the form.
del = document.getElementById('form-edit-climb');
del.removeChild(del.firstChild);

buttonEdit = document.getElementById('btn-edit');
buttonEdit.classList.remove('d-none');

<div class="greeting-bar">
  <div class="greeting-name">
    <% if user_signed_in? %>
      Hello <%= current_user.first_name %>
    <% else %>
      Have a safe climb, friend!
    <% end %>
  </div>
  <div class="greeting-date">
    <%= Date.today.strftime("%a %b %d, %Y") %>
  </div>
</div>
